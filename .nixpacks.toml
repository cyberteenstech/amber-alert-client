[phases.setup]
nixPkgs = [
    "nodejs-20.x",
    "pnpm"
]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]
env = {
    "NEXT_TELEMETRY_DISABLED" = "1",
    "CI" = "true"
}

[start]
cmd = "pnpm start"